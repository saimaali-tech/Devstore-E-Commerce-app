apiVersion: v1
kind: Service
metadata:
  name: devstore-web-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: devstore-web
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30001   # fixed port for easier access